openapi: 3.0.0
servers:
  - url: https://foot-api-server.com
info:
  version: latest
  title: PROG3 - TD2
paths:
  /matches/{matchId}:
    get:
      tags:
        - Matches
      summary: Get match by Id
      operationId: getMatchById
      responses:
        '200':
          description: Match
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Match'
  /matches:
    get:
      tags:
        - Matches
      summary: Get all matches
      operationId: getMatches
      responses:
        '200':
          description: List of Matches
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Match'
  /matches/{matchId}/goals:
    post:
      tags:
        - Matches
      summary: Add some goals to a match
      parameters:
        - name: matchId
          in: path
          schema:
            type: integer
          required: true
      description: Be careful! A guardian player can not score a goal.
      operationId: addGoals
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/Scorer'
      responses:
        '200':
          description: The updated match with new scores
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Match'
  /players:
    get:
      tags:
        - Players
      summary: Get all players
      operationId: getPlayers
      responses:
        '200':
          description: List of Players
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Player'
    post:
      tags:
        - Players
      summary: Add players
      description: Be careful! A player must have a team.
      operationId: addPlayers
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/PlayerWithoutId'
      responses:
        '200':
          description: The Players created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Player'
    put:
      tags:
        - Players
      summary: Update players
      operationId: putPlayers
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/PlayerWithoutTeam'
      responses:
        '200':
          description: The Players updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Player'
components:
  schemas:
    Team:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
    Player:
      properties:
        teamName:
          type: string
      allOf:
        - $ref: "#/components/schemas/PlayerWithoutTeam"
    PlayerWithoutTeam:
      properties:
        id:
          type: integer
        name:
          type: string
        isGuardian:
          type: boolean
    PlayerWithoutId:
      properties:
        name:
          type: string
        isGuardian:
          type: boolean
        teamName:
          type: string
    Match:
      type: object
      properties:
        id:
          type: integer
        datetime:
          type: string
          format: datetime
        stadium:
          type: string
        teamA:
          allOf:
            - $ref: '#/components/schemas/PlayedTeam'
        teamB:
          allOf:
            - $ref: '#/components/schemas/PlayedTeam'
    PlayedTeam:
      type: object
      properties:
        team:
          $ref: '#/components/schemas/Team'
        goals:
          type: integer
        scorers:
          type: array
          items:
            $ref: '#/components/schemas/Scorer'
    Scorer:
      type: object
      properties:
        player:
          $ref: '#/components/schemas/PlayerWithoutTeam'
        isOG:
          type: boolean
        scoreTime:
          type: integer
